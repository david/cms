<Layouts.flash_group flash={@flash} />

<div class="flex flex-col items-center justify-center h-screen">
  <.link :if={@liturgy} href={~p"/liturgies/#{@liturgy.id}"} class="btn btn-primary">
    {gettext("Liturgy")}
  </.link>
  <.link navigate={~p"/songs"} class="btn btn-secondary mt-4">Songs</.link>
</div>
